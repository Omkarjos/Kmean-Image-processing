# K-Means Image Compression / Color Quantization

Applies K-Means clustering to RGB pixels to reduce an image to a smaller color palette.

## Install
pip install -r requirements.txt

## Run
python main.py --input sample.jpg --colors 16

Place any RGB image named `sample.jpg` in this folder.
